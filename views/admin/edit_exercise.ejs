<!DOCTYPE html>
<html lang="et">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <% include ../imports/head.ejs %>
    <script src="/js/admin.min.js"></script>
    <title><%=data.title %> - Ülesanne - <%= pageTitle %></title>
  </head>
  <body>
    <% include ../imports/sidenav.ejs %> <% include ../imports/navbar.ejs %>
    <div class="container">
      <form
        action="/admin/edit_exercise/<%= cat_id %>:<%= exercise.id %>/<%= type %>"
        method="POST"
        id="edit-exercise"
      >
        <input type="submit" value="Salvesta" class="btn green" />
        <input type="text" name="title" value="<%= exercise.title %>" />
        <textarea
          name="data"
          id="edit-exercise-data"
          cols="30"
          rows="10"
          class="materialize-textarea"
        >
<%= JSON.stringify(exercise.data, null, 2) %></textarea
        >
      </form>
    </div>
    <script>
      ctrlEnterSave('#edit-exercise-data', () => {
        $('#edit-exercise').submit();
      });
    </script>
  </body>
</html>
